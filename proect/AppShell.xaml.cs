namespace proect
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();

            // ЭТО НЕ НУЖНО - УБЕРИТЕ ЭТИ СТРОКИ
            // Routing.RegisterRoute("MainPage", typeof(MainPage));
            // Routing.RegisterRoute("SettingsPage", typeof(NewPage1));
        }
    }
}